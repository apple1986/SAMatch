# arguments for dataset
dataset: mrliver
nclass: 2
crop_size: 256
data_root: /home/gxu/proj1/smatch/data/MRliver

# arguments for training
epochs: 300
batch_size: 12 # per GPU x 1 GPU
lr: 0.01
conf_thresh: 0.95
